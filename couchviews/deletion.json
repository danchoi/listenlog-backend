{
   "_id":"design/deletion",
   "views": {
       "all_for_user": 
         { 
              "map": "function(doc) { 
                  if (doc.user) 
                      emit([doc.user.UDID, doc._rev], null); 
                }" 
         },
   }
}
